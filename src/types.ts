export type NewsItemType = {
  id: string,
  title: string,
  url: string,
  icon: string,
}

export type QuoteType = {
  id: string,
  title: string,
  value: string,
  variation: string,
  url: string,
}
